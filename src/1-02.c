/**
 * 1-2.c
 * Experiment to find out what happens when prints's
 * argument string contains \c, where c is some character
 * not listed above.
 */
#include <stdio.h>

void main() { printf("\a"); }

/**
 * Solution: Different characters just represent different
 * escape sequences
 */
